package nl.b3p.kaartenbalie.core.server;

public class DCPType {
    
    private HTTP http;
    
    public HTTP getHTTP(HTTP http) {
        return http;
    }
    
    public void setHTTP(HTTP http) {
        this.http = http;
    }
}